services:
  - type: web
    name: waitrose-scraper
    env: docker
    plan: free
    region: oregon
    dockerfilePath: ./Dockerfile
    port: 10000
    buildCommand: echo "Docker build handled by Dockerfile"
    # Start command is ignored for Docker
    startCommand: ""
